[package]
name = "psect-core"
version = "0.1.0"
edition = "2024"
description = "probabilistic regression search — entropy-minimizing bisection for flaky tests"
license = "MIT"
repository = "https://github.com/skurella/psect"

[dependencies]
env_logger = "0.11.9"
log = "0.4.29"
rand = "0.10.0"
